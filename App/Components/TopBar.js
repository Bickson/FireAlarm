
import React from 'react'
import {View, Text} from 'react-native'
import styles from './Styles/TopBarStyle'

export default (props) => {
  return <View style={styles.container}>
    <Text style={styles.title}>SMOKE SENSORS</Text>
  </View>
}